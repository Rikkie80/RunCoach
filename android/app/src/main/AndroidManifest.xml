<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.runcoach">

    <!-- ============================================ -->
    <!-- LOCATION PERMISSIONS (ANDROID 16 / API 35 COMPATIBLE) -->
    <!-- ============================================ -->
    
    <!-- Fine location - required for precise GPS tracking -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    
    <!-- Coarse location - fallback -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Background location - for Android 10-15 -->
    <!-- On Android 16 (API 35), this requires explicit user consent -->
    <uses-permission 
        android:name="android.permission.ACCESS_BACKGROUND_LOCATION"
        android:maxSdkVersion="34" />
    
    <!-- Foreground service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    
    <!-- Wake lock - to keep CPU awake during tracking -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Vibrate - for notifications -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:label="Run Coach"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="false"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        tools:targetApi="35">

        <!-- ============================================ -->
        <!-- MAIN ACTIVITY -->
        <!-- ============================================ -->
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            
            <meta-data
                android:name="io.flutter.embedding.android.SplashScreenDrawable"
                android:resource="@drawable/launch_background" />
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============================================ -->
        <!-- GEOLOCATOR SERVICES -->
        <!-- ============================================ -->
        <service
            android:name="com.baseflow.geolocator.service.LocationService"
            android:foregroundServiceType="location"
            android:exported="false" />
            
        <service
            android:name="com.baseflow.geolocator.service.LocationForegroundService"
            android:foregroundServiceType="location"
            android:exported="false" />
            
        <!-- ============================================ -->
        <!-- GEOLOCATOR NOTIFICATION CHANNEL -->
        <!-- ============================================ -->
        <meta-data
            android:name="com.baseflow.geolocator.service.notification_channel"
            android:value="@string/geolocator_notification_channel" />
            
        <meta-data
            android:name="com.baseflow.geolocator.service.notification_channel_description"
            android:value="@string/geolocator_notification_channel_description" />
            
        <meta-data
            android:name="com.baseflow.geolocator.service.notification_icon"
            android:resource="@mipmap/ic_launcher" />
            
        <meta-data
            android:name="com.baseflow.geolocator.service.notification_title"
            android:value="@string/location_service_notification_title" />
            
        <meta-data
            android:name="com.baseflow.geolocator.service.notification_text"
            android:value="@string/location_service_notification_text" />
            
        <meta-data
            android:name="com.baseflow.geolocator.service.foreground_service_type"
            android:value="location" />
    </application>
</manifest>
